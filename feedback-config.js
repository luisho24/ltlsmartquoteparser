// Public configuration only. Never put API keys, tokens or secrets in this file.
// Set endpoint to the verified HTTPS origin of the deployed PartsPilot service.
// Intentionally disabled until deployment succeeds; no false success messages.
window.LTL_FEEDBACK_CONFIG = Object.freeze({ endpoint: '' });
